sentences:
  - turn (on|off) the {light}
  - set light to {colors}
  - in:
      - <do> something[ else]
    out: something completely different
lists:
  colors:
    values:
      - red
      - green
      - blue
  light:
    values:
      - light
      - in: lamp
        out: light
expansion_rules:
  do: do|try
no_correct_patterns:
  - "^draw .+$"
unknown_text: "Text sent for unknown commands"
