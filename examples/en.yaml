sentences:
  - turn (on|off) the {light}
  - set light to {colors}
  - in:
      - <do> something[ else]
    out: something completely different
lists:
  colors:
    - red
    - green
    - blue
  light:
    - light
    - in: lamp
      out: light
expansion_rules:
  do: do|try
