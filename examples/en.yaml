sentences:
  - turn (on|off) the light
  - set light to {colors}
  - in:
      - do something
      - do something else
    out: something completely different
lists:
  colors:
    - red
    - green
    - blue
